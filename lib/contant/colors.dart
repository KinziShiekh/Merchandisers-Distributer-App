import 'package:flutter/material.dart';

// Define a class to hold custom colors
class AppColors {
  static const MainColor = Color(0xFFf16611);
  static const Background = Color.fromARGB(255, 255, 255, 255);
  static const AccentColor = Color(0xFF4CAF50); // Green for increment
  static const ErrorColor =
      Color(0xFFE57373); // Soft red for remove/decrement// Example orange color
}
